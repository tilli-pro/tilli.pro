"use client";

// You can use this component if you need to test some client component functionality but don't want to make a whole new file for the "use client" directive

const ClientTest: React.FC = () => {
  return null;
};

export default ClientTest;
